[arch]
packages = [
  # Python
  "uv",

  # C++
  "gcc",
  "gdb",
  "valgrind",
  "clang",
  "cmake",

  # Node js
  "mise",

  # Docker
  "docker",
  "docker-compose",
]

[mise]
packages = [{name = "node", options = { version = "lts" }}]

# [npm]
# packages = [
#   "yarn",
# ]